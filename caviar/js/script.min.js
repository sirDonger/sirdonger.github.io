$(function () {
    window.SetVideoBG = function (f) {
        var e = 16 / 9;

        function d() {
            var k = Math.floor(f.width());
            var j = Math.floor(f.outerHeight());
            var g = Math.floor($(window).width());
            if (g < k) {
                g = k
            }
            var m = Math.floor(g / e);
            if (m < j) {
                m = j;
                g = Math.floor(m * e)
            }
            var l = 0, h = 0;
            if ((j - m) < 0) {
                l = Math.floor((j - m) / 2)
            }
            if ((k - g) < 0) {
                h = Math.floor((k - g) / 2)
            }
            f.find(".video_bg").css({height: m, width: g, left: h, top: l,})
        }

        d();
        $(window).resize(function () {
            d()
        })
    };
    $(".section").each(function () {
        if ($(this).find(".video_bg").size()) {
            SetVideoBG($(this))
        }
    });
    $("#wrapper").find("iframe").each(function () {
        var e = $(this).attr("src");
        if ($(this).parents(".form_html, .html").size()) {
            return false
        }
        if (e.indexOf("https://www.youtube.com/embed/") > -1) {
            var d = e.substring(30);
            $(this).replaceWith('<div class="video_holder" data-video="' + $(this).attr("src") + '" style="width:' + $(this).width() + "px; height:" + $(this).height() + "px; background-image: url(https://i.ytimg.com/vi/" + d + '/hqdefault.jpg);"></div>')
        }
    });
    $(".video_holder").on("click", function () {
        var d = $(this).data("video");
        $(this).replaceWith('<iframe  allowfullscreen src="' + d + '?autoplay=1"></iframe>')
    });
    $(".section318 .accordion .line.caption, .section319 .accordion .line.caption").click(function () {
        $(this).parent().parent().find(".line.option, .line.btn").hide();
        $(this).parent().find(".line.option, .line.btn").show()
    });
    $("body").on("click", ".popup_thanks_close, .popup_form_close", function () {
        $(".popup_thanks").hide();
        $(".popup_form").hide()
    });
    $("body").on("click", ".popup_form", function (d) {
        if ($(d.target).closest(".popup_form_inner").length === 0) {
            $(".popup_form").hide()
        }
    });
    $("body").on("click", ".popup_thanks", function (d) {
        if ($(d.target).closest(".popup_thanks_inner").length === 0) {
            $(".popup_thanks").hide()
        }
    });
    if ($(".section153").size()) {
        $(".section153").each(function () {
            SetVideoBG($(this))
        })
    }
    if ($(".section52").size()) {
        $(".section52 .logo").hover(function () {
            $(this).children("img").removeClass("grayscale")
        }, function () {
            $(this).children("img").addClass("grayscale")
        })
    }
    if ($(".section19").size()) {
        $(".section19 .policy").click(function () {
            $("#policy").remove();
            $("body").append('<div id="policy">\n				<div class="close  close_x">X</div>\n				<h1>Политика конфиденциальности</h1>\n				<p>\n				Данный сайт уважает Ваше право и соблюдает конфиденциальность при заполнении, передаче и хранении Ваших конфиденциальных сведений.\n				Размещение заявки на данном сайте означает Ваше согласие на обработку данных и дальнейшей передачи ваших контактных данных нашей компании.\n				Под персональными данными подразумевается информация, относящаяся к субъекту персональных данных, в частности имя, контактные реквизиты (адрес электронной почты) и иные данные, относимые Федеральным законом от 27 июля 2006 года № 152-ФЗ «О персональных данных» к категории персональных данных.\n				Целью обработки персональных данных является информирование об оказываемых услугах нашей компании.\n				Есть вопросы? Пишите нам на ' + window.tobiz.owner_email + '.</p>\n				<div class="close close_btn">Закрыть</div></div>');
            $("#policy .close_x, #policy .close_btn").click(function () {
                $("#policy").remove()
            })
        })
    }
    if ($(".section119").size()) {
        $(".section119 .policy").click(function () {
            $("#policy").remove();
            $("body").append('<div id="policy">\n				<div class="close  close_x">X</div>\n				<h1>Политика конфиденциальности</h1>\n				<p>\n				Данный сайт уважает Ваше право и соблюдает конфиденциальность при заполнении, передаче и хранении Ваших конфиденциальных сведений.\n				Размещение заявки на данном сайте означает Ваше согласие на обработку данных и дальнейшей передачи ваших контактных данных нашей компании.\n				Под персональными данными подразумевается информация, относящаяся к субъекту персональных данных, в частности имя, контактные реквизиты (адрес электронной почты) и иные данные, относимые Федеральным законом от 27 июля 2006 года № 152-ФЗ «О персональных данных» к категории персональных данных.\n				Целью обработки персональных данных является информирование об оказываемых услугах нашей компании.\n				Есть вопросы? Пишите нам на ' + window.tobiz.owner_email + '.</p>\n				<div class="close close_btn">Закрыть</div></div>');
            $("#policy .close_x, #policy .close_btn").click(function () {
                $("#policy").remove()
            })
        })
    }
    $("body").on("click", ".section119 .policy2", function () {
        console.log("click");
        $(this).parent().parent().children(".policy2_popup").toggle()
    });
    $("body").on("click", ".product_card", function () {
        if ($(this).parent().parent().children(".extra_info_block_wrapper").eq($(this).data("id")).is(":visible")) {
            $(this).parent().parent().children(".extra_info_block_wrapper").hide()
        } else {
            $(this).parent().parent().children(".extra_info_block_wrapper").hide();
            $(this).parent().parent().children(".extra_info_block_wrapper").eq($(this).data("id")).show();
            $("body").addClass("modal")
        }
    });
    $("body").on("click", ".extra_info_block .close", function () {
        $(this).parent().parent().hide();
        $("body").removeClass("modal")
    });
    $("body").on("click", ".extra_info_block_wrapper", function (d) {
        if ($(d.target).closest(".extra_info_block").length === 0) {
            if (typeof(window.tobiz.editor) === "undefined") {
                $(".extra_info_block_wrapper").hide();
                $("body").removeClass("modal")
            }
        }
    });
    if ($(".section143 .overlay_image_box, .section148 .overlay_image_box, .section147 .overlay_image_box, .section313 .overlay_image_box, .section315 .overlay_image_box").size()) {
        $(".section143 .overlay_image_box, .section148 .overlay_image_box, .section147 .overlay_image_box, .section313 .overlay_image_box, .section315 .overlay_image_box").click(function () {
            if ($(this).attr("data-link") != "" && $(this).attr("data-link") != "undefined") {
                window.open($(this).attr("data-link"), "_blank");
                return false
            }
        })
    }
    if ($(".section41 .overlay_image_box, .section42 .overlay_image_box, .section43 .overlay_image_box, .section44 .overlay_image_box, .section45 .overlay_image_box, .section46 .overlay_image_box, .section47 .overlay_image_box, .section48 .overlay_image_box").size()) {
        $(".section41 .overlay_image_box, .section42 .overlay_image_box, .section43 .overlay_image_box, .section44 .overlay_image_box, .section45 .overlay_image_box, .section46 .overlay_image_box, .section47 .overlay_image_box, .section48 .overlay_image_box").click(function () {
            var e = $(this).closest(".section");
            if (e.hasClass("img_zoom")) {
                var g = $(this).parent().children().attr("src");
                var f = g.split("/");
                var d = "/img/800x600/" + f[3];
                console.log(d);
                if ($("#img_zoomer_bg").size()) {
                    $("#img_zoomer_bg").remove()
                }
                $("body").append('<div id="img_zoomer_bg"><div id="img_zoomer" style="background-image:url(' + d + ')"></div><div id="img_zoomer_close">X</div></div>')
            }
        })
    }
    if ($(".section141 .overlay_image_box, .section142 .overlay_image_box, .section143 .overlay_image_box, .section144 .overlay_image_box, .section145 .overlay_image_box, .section146 .overlay_image_box, .section147 .overlay_image_box, .section148 .overlay_image_box, .section313 .overlay_image_box, .section315 .overlay_image_box ").size()) {
        $(".section141 .overlay_image_box, .section142 .overlay_image_box, .section143 .overlay_image_box, .section144 .overlay_image_box, .section145 .overlay_image_box, .section146 .overlay_image_box, .section147 .overlay_image_box, .section148 .overlay_image_box, .section313 .overlay_image_box , .section315 .overlay_image_box ").click(function () {
            var e = $(this).closest(".section");
            if (e.hasClass("img_zoom")) {
                var g = $(this).parent().children().attr("src");
                var f = g.split("/");
                var d = "/img/800x600/" + f[3];
                console.log(1);
                console.log(d);
                if ($("#img_zoomer_bg").size()) {
                    $("#img_zoomer_bg").remove()
                }
                $("body").append('<div id="img_zoomer_bg"><div id="img_zoomer" style="background-image:url(' + d + ')"></div><div id="img_zoomer_close">X</div></div>')
            }
        })
    }
    if ($(".section128 .image1, .section125 .image1, .section124 .image1, .section149 .image1, .section150 .image1, .section200 .image1, .section201 .image1, .section138 .image, .section250 .image1").size()) {
        $(".section128 .image1, .section128 .image2, .section128 .image3,  .section125 .image1,  .section125 .image2,  .section124 .image1,  .section124 .image2,  .section124 .image3, .section149 .image1, .section149 .image2, .section149 .image3, .section150 .image1, .section150 .image2, .section200 .image1, .section200 .image2, .section200 .image3, .section200 .image4, .section201 .image1, .section201 .image2, .section201 .image3, .section201 .image4, .section201 .image5, .section138 .image, .section250 .image1, .section250 .image2").click(function () {
            $(".show_extra_info").removeClass("currnet");
            $(this).addClass("currnet");
            var h = $(this).closest(".arr1");
            var f = 0;
            $.each(h.find(".show_extra_info"), function (j, l) {
                if ($(l).hasClass("currnet")) {
                    f = j
                }
            });
            if ($(this).hasClass("show_extra_info")) {
                $(this).parent().parent().children(".extra_info_block_wrapper").eq(f).show();
                $("body").addClass("modal");
                return true
            }
            if ($(this).hasClass("extra_image")) {
                return false
            }
            var e = $(this).closest(".section");
            if (e.hasClass("img_zoom")) {
                var k = $(this).children().attr("src");
                var g = k.split("/");
                var d = "/img/800x600/" + g[3];
                console.log(2);
                console.log(d);
                if ($("#img_zoomer_bg").size()) {
                    $("#img_zoomer_bg").remove()
                }
                $("body").append('<div id="img_zoomer_bg"><div id="img_zoomer" style="background-image:url(' + d + ')"></div><div id="img_zoomer_close">X</div></div>')
            }
        })
    }
    if ($(".section151 .image1").size()) {
        $(".section151 .image1, .section151 .image2, .section151 .image3, .section151 .image4").click(function () {
            var e = $(this).closest(".section");
            if (e.hasClass("img_zoom")) {
                var g = $(this).children().attr("src");
                var f = g.split("/");
                var d = "/img/400x560/" + f[3];
                console.log(d);
                if ($("#img_zoomer_bg").size()) {
                    $("#img_zoomer_bg").remove()
                }
                $("body").append('<div id="img_zoomer_bg"><div id="img_zoomer" class="portret" style="background-image:url(' + d + ')"></div><div id="img_zoomer_close" class="portret">X</div></div>')
            }
        })
    }
    $("body").on("click", "#img_zoomer_bg ,#img_zoomer_clocse", function () {
        if ($("#img_zoomer_bg").size()) {
            $("#img_zoomer_bg").remove()
        }
    });
    $(".btn1, .btn2, .btn3, .btn4,  .btn5, .btn1x, .btn2x, .btn3x, .btn4x,  .btn5x").each(function () {
        if ($(this).css("background-color") == "rgb(255, 255, 255)") {
        }
    });
    window.basket = {
        status: 0, getData: function () {
            var d = JSON.parse(localStorage.getItem("basket"));
            if (d == null) {
                return []
            } else {
                return d
            }
        }, saveData: function (d) {
            try {
                localStorage.setItem("basket", JSON.stringify(d))
            } catch (f) {
                if (f == QUOTA_EXCEEDED_ERR) {
                    alert("Ошибка добавления товара в корзину")
                }
            }
        }, addItem: function (d) {
            var e = this.getData();
            var f = false;
            $.each(e, function (g, h) {
                if (h.title == d.title && h.price == d.price) {
                    f = true;
                    if (typeof(h.quantity) === "undefined") {
                        h.quantity = 1
                    } else {
                        h.quantity = (parseInt(h.quantity));
                        h.quantity++
                    }
                }
            });
            if (!f) {
                e.push(d)
            }
            this.saveData(e);
            this.renderForm();
            this.renderBtn();
            this.showBtn()
        }, removeItem: function (e) {
            var f = this.getData();
            var d = false;
            if (f) {
                $.each(f, function (g, h) {
                    if (d) {
                        return true
                    }
                    if (h.title == e.title && h.price == e.price) {
                        f.splice(g, 1);
                        d = true
                    }
                })
            }
            this.saveData(f)
        }, updateItem: function (e) {
            var f = this.getData();
            var d = false;
            if (f) {
                $.each(f, function (g, h) {
                    if (d) {
                        return true
                    }
                    if (h.title == e.title && h.price == e.price) {
                        h.quantity = e.quantity;
                        d = true
                    }
                })
            }
            this.saveData(f)
        }, clean: function () {
            this.saveData([])
        }, countItems: function () {
            return this.getData().length
        }, countSum: function () {
            var d = 0;
            $.each(this.getData(), function (e, f) {
                d += parseInt(f.quantity) * parseInt(f.price)
            });
            return d
        }, btnEventListener: function () {
            var d = this;
            $("body").on("click", "#basket_btn", function () {
                d.showForm()
            })
        }, renderBtn: function () {
            $("#basket_btn").remove();
            $("body").append('<div id="basket_btn"  style="display:none"> <i class="fa fa-shopping-cart"></i> Корзина (' + this.countItems() + ")</div>");
            this.init()
        }, hideBtn: function () {
            $("#basket_btn").hide()
        }, showBtn: function () {
            $("#basket_btn").show()
        }, formEventListener: function () {
            var d = this;
            $("body").on("click", "#basket_form_close", function () {
                d.hideForm()
            });
            $("body").on("click", "#basket_left .remove", function () {
                var g = $(this).parent().children(".title").text();
                var f = $(this).parent().children(".price").text();
                var e = {title: g, price: f};
                console.log(e);
                d.removeItem(e);
                d.renderForm();
                d.showForm()
            });
            $("body").on("keyup", "#basket_left .quantity input", function (h) {
                this.value = this.value.replace(/\D/gi, "").replace(/^0+/, "");
                if (this.value == "") {
                    this.value = 0
                }
                var k = $(this).parent().parent().children(".title").text();
                var g = $(this).parent().parent().children(".price").text();
                var j = $(this).val();
                var f = {title: k, price: g, quantity: j};
                var e = parseInt(g) * parseInt(j);
                $(this).parent().parent().children(".itogo").text(e);
                d.updateItem(f);
                $("#basket_form_itogo").children("span").text(d.countSum());
                $('#basket_make_order textarea[name="order"]').text(JSON.stringify(d.getData()));
                $('#basket_make_order input[name="amount"]').text(d.countSum())
            });
            $("body").on("click", "#basket_continue", function (e) {
                d.hideForm()
            })
        }, renderForm: function () {
            var d = this;
            $("#basket_form_bg").remove();
            $("body").append('<div id="basket_form_bg" style="display:none">\n                    <div id="basket_form_inner">\n                        <div id="basket_form_close">X</div>\n                        <div id="basket_left"></div>\n                        <div id="basket_right"></div>\n                        <div class="clear"></div>\n                    </div>\n                </div>');
            var e = this.getData();
            $("#basket_left").append('<div class="item caption">\n                        <div class="image">Фото</div>\n                        <div class="title">Название</div>\n                        <div class="quantity">Количество</div>\n                        <div class="price">Цена, за ед.</div>\n                        <div class="itogo">Цена</div>\n                        <div class="clear"></div>\n                    </div>');
            $.each(e, function (f, g) {
                var h = "";
                if (g.image != "") {
                    h = '<img src="' + g.image + '" />'
                }
                $("#basket_left").append('<div class="item" data-id="' + f + '">\n                            <div class="image">' + h + '</div>\n                            <div class="title">' + g.title + '</div>\n                            <div class="quantity"><input type="numper" value="' + g.quantity + '" /></div>\n                            <div class="price">' + g.price + '</div>\n                            <div class="itogo">' + (g.price * g.quantity) + '</div>\n                            <div class="remove"><i class="fa fa-trash"></i></div>\n                            <div class="clear"></div>\n                        </div>')
            });
            $("#basket_left").append('<div id="basket_form_itogo">Итого: <span>' + this.countSum() + "</span></div>");
            $("#basket_right").append('<div id="basket_title">Оформить заказ</div>');
            $("#basket_right").append('<div id="basket_make_order"><form action="handler.php" enctype="multipart/form-data" method="post">\n                        <input type="hidden" name="action" value="basket" />\n                        <input type="hidden" name="amount" value="' + d.countSum() + '" />\n                        <div><input type="text" name="email" required="required" placeholder="Введите E-mail" /></div>\n                        <div><input type="text" name="tel"  required="required" placeholder="Введите телефон" /></div>\n                        <div><textarea name="comment" placeholder="Адрес доставки (комментарий)" ></textarea></div>\n                        <div><textarea name="order" style="display:none">' + JSON.stringify(d.getData()) + '</textarea></div>\n                        <div><input type="submit" value="Оформить заказ" /></div>\n                    </form></div>');
            $("#basket_make_order form").append('<div id="basket_continue">Продолжить покупки</div>');
            this.init()
        }, hideForm: function () {
            $("#basket_form_bg").hide();
            this.showBtn()
        }, showForm: function () {
            $("#basket_form_bg").show();
            this.hideBtn()
        }, sendForm: function () {
        }, init: function () {
            if (!this.status) {
                this.status = 1;
                this.formEventListener();
                this.btnEventListener()
            }
        }
    };
    if (window.basket.countItems() > 0 && window.tobiz.t >= 3) {
        window.basket.renderForm();
        window.basket.renderBtn();
        window.basket.showBtn();
        window.basket.init()
    }
    $("div.btn1, div.btn2, div.btn3, div.btn4,  div.btn5").click(function () {
        var e = $(this).closest(".section");
        var d = this;
        if ($(this).hasClass("add_basket") && window.tobiz.t >= 3) {
            var k = $(this).parent().find(".title1, .title2, .title3, .title4, .title5").text();
            var h = $(this).parent().find(".price1, .price2, .price3, .price4, .price5").text();
            if (e.hasClass("section125") || e.hasClass("section126")) {
                var j = $(this).parent().parent().find(".image1 img, .image2 img, .image3 img, .image4 img, .image5 img").attr("src")
            } else {
                var j = $(this).parent().find(".image1 img, .image2 img, .image3 img, .image4 img, .image5 img").attr("src")
            }
            h = parseInt(h.replace(/\D+/g, ""));
            if (!j) {
                j = ""
            } else {
                var f = j.split("/");
                j = "/img/100x100/" + f[f.length - 1]
            }
            var g = {title: k, price: h, image: j, quantity: 1};
            window.basket.addItem(g);
            window.basket.renderForm();
            window.basket.showForm()
        } else {
            if (e.find(".arr1").size()) {
                if (e.hasClass("section312")) {
                    $(this).closest(".arr1").find(".popup_form").show()
                } else {
                    if (e.hasClass("section318") || e.hasClass("section319")) {
                        $(d).parent().find(".popup_form").show()
                    } else {
                        if (e.hasClass("section126")) {
                            if ($(d).hasClass("btn1")) {
                                console.log("show_form1");
                                $(this).parent().find(".popup_form").eq(0).show()
                            }
                            if ($(d).hasClass("btn2")) {
                                console.log("show_form2");
                                $(this).parent().find(".popup_form").eq(1).show()
                            }
                        } else {
                            $(this).parent().find(".popup_form").show()
                        }
                    }
                }
            } else {
                e.find(".popup_form").show()
            }
        }
    });
    $(".btn1x, .btn2x, .btn3x, .btn4x,  .btn5x").click(function () {
        $(".extra_info_block_wrapper").hide();
        for (i = 0; i <= 5; i++) {
            if ($(this).parent().children(".extra_info" + i).size()) {
                $(this).closest(".arr1").find(".btn" + i).click()
            }
        }
    });
    $(".btn1, .btn2, .btn3, .btn4,  .btn5, .submit_btn, .btn1x, .btn2x, .btn3x, .btn4x,  .btn5x").hover(function () {
        if ($(this).hasClass("surround")) {
            var e = $(this).css("background-color");
            $(this).attr("data-color", e);
            var d = "#fff";
            if (e == "rgb(255, 255, 255)") {
                d = "#3D3D3D"
            }
            $(this).css({backgroundColor: d, color: e,})
        } else {
            var e = $(this).css("color");
            $(this).attr("data-color", e);
            var d = "#fff";
            if (e == "rgb(255, 255, 255)") {
                d = "#3D3D3D"
            }
            if (e == "rgb(255, 255, 255)") {
                e = "rgb(0, 0, 0,0.5    )";
                d = "#FFF"
            }
            $(this).css({backgroundColor: e, color: d,})
        }
        $(this).addClass("hover")
    }, function () {
        if ($(this).hasClass("surround")) {
            var d = $(this).data("color");
            $(this).css({backgroundColor: d, color: "#fff",});
            if ($(this).css("background-color") == "rgb(255, 255, 255)") {
                $(this).css({color: "#3D3D3D"})
            }
        } else {
            var d = $(this).data("color");
            $(this).css({color: d, backgroundColor: "transparent"})
        }
        $(this).removeAttr("date-color");
        $(this).removeClass("hover")
    });
    setInterval(function () {
        if ($(".objtimer").size()) {
            $(".objtimer").each(function () {
                var k = $(this).data("type"), q = $(this).data("dd"), h = $(this).data("dm"), s = $(this).data("dy"),
                    e = $(this).data("monthly"), m = $(this).data("weekly"), p = $(this).data("hr"),
                    g = $(this).data("min"), l = new Date();
                if (k == "date") {
                    var o = new Date(s, h - 1, q, p, g, 0, 0)
                }
                if (k == "monthly") {
                    var o = new Date(l.getFullYear(), l.getMonth(), q, p, g, 0, 0);
                    if (l > o) {
                        o = new Date(l.getFullYear(), l.getMonth() + 1, q, p, g, 0, 0)
                    }
                }
                if (k == "weekly") {
                    var o = new Date(l.getFullYear(), l.getMonth(), l.getDate(), p, g, 0, 0);
                    o.setDate(l.getDate() + (m + 7 - l.getDay()) % 7)
                }
                if (k == "daily") {
                    var o = new Date(l.getFullYear(), l.getMonth(), l.getDate(), p, g, 0, 0);
                    if (l > o) {
                        o = new Date(l.getFullYear(), l.getMonth(), l.getDate() + 1, p, g, 0, 0)
                    }
                }
                if (l > o) {
                    if (window.tobiz.editor) {
                        $(this).children(".days").text("00");
                        $(this).children(".hrs").text("00");
                        $(this).children(".min").text("00");
                        $(this).children(".sec").text("00")
                    } else {
                        $(this).closest(".section").addClass("invis")
                    }
                } else {
                    var r = parseInt((o.getTime() - l.getTime()) / 1000), n = Math.floor(r / (24 * 3600)),
                        j = Math.floor((r - n * 24 * 3600) / (3600)),
                        d = Math.floor((r - n * 24 * 3600 - j * 3600) / (3600 / 60)),
                        f = Math.floor((r - n * 24 * 3600 - j * 3600 - d * (3600 / 60)) / (3600 / (60 * 60)));
                    if (n < 10) {
                        n = "0" + n
                    }
                    if (j < 10) {
                        j = "0" + j
                    }
                    if (d < 10) {
                        d = "0" + d
                    }
                    if (f < 10) {
                        f = "0" + f
                    }
                }
                $(this).children(".days").text(n);
                $(this).children(".hrs").text(j);
                $(this).children(".min").text(d);
                $(this).children(".sec").text(f)
            })
        }
    }, 1000);
    setInterval(function () {
        if ($("[data-map-obj]").size()) {
            $("[data-map-obj]").each(function (e) {
                var g = JSON.parse(_.unescape($(this).attr("data-map-obj")));
                var d = JSON.parse(_.unescape($(this).attr("data-map-center")));
                $(this).removeAttr("data-map-obj");
                $(this).removeAttr("data-map-center");
                var h = $(this).children(".map_inner")[0];

                function f() {
                    var j;
                    j = new ymaps.Map(h, d);
                    d.controls = ["zoomControl"];
                    _.each(g, function (l, k) {
                        var m = ymaps.geocode(l.address);
                        m.then(function (o) {
                            var n = new ymaps.Placemark(o.geoObjects.get(0).geometry.getCoordinates(), {}, {
                                preset: "islands#icon",
                                iconColor: l.color
                            });
                            j.geoObjects.add(n)
                        }, function (n) {
                        })
                    })
                }

                $.ajax({
                    url: "https://api-maps.yandex.ru/2.1/?lang=ru_RU",
                    dataType: "script",
                    cache: true,
                    success: function () {
                        ymaps.ready(function () {
                            f()
                        })
                    }
                })
            })
        }
    }, 200);
    setInterval(function () {
        if ($(".fixed_top").size()) {
            var f = $(".fixed_top").size();
            var d = 0;
            var g = 0;
            var e = 100 + f;
            $(".fixed_top").each(function () {
                d += $(this).outerHeight();
                $(this).css({top: g + "px"});
                $(this).css({zIndex: e});
                g += $(this).outerHeight();
                e--
            });
            $("#wrapper").css({paddingTop: d + "px"})
        } else {
            $("#wrapper").css({paddingTop: 0 + "px"})
        }
    }, 200);
    $(window).scroll(function () {
        $(".fixed_top").css({left: "-" + $(window).scrollLeft() + "px"})
    });
    if (window.location.search == "?rk_pay=success") {
        alert("Оплата успешно завершена!");
        window.location.search = ""
    }
    if (window.location.search == "?rk_pay=fail") {
        alert("Оплата не завершена!");
        window.location.search = ""
    }
    $(".section302 .left.style2 ul li a, .section302 .left.style4 ul li a").hover(function () {
        $(this).parent().addClass("hover")
    }, function () {
        $(this).parent().removeClass("hover")
    });
    $("body").on("click", ".section304 .spoiler_toggle", function () {
        if ($(this).parent().hasClass("close")) {
            $(this).text("-")
        }
        if ($(this).parent().hasClass("open")) {
            $(this).text("+")
        }
        $(this).parent().toggleClass("open");
        $(this).parent().toggleClass("close")
    });
    $("body").on("click", ".section305 .spoiler_toggle", function () {
        if ($(this).parent().hasClass("close")) {
            $(this).text("-")
        }
        if ($(this).parent().hasClass("open")) {
            $(this).text("+")
        }
        $(this).parent().toggleClass("open");
        $(this).parent().toggleClass("close")
    });
    $("body").on("click", ".section304 .spoiler_title", function () {
        if (typeof(window.tobiz.editor) === "undefined") {
            $(this).parent().toggleClass("open");
            $(this).parent().toggleClass("close")
        }
    });
    $("body").on("click", ".section305 .spoiler_title", function () {
        if (typeof(window.tobiz.editor) === "undefined") {
            $(this).parent().toggleClass("open");
            $(this).parent().toggleClass("close")
        }
    });
    if ($(".section304 .spoiler").size() && typeof(window.tobiz.editor) === "undefined") {
        $(".section304 .spoiler").each(function () {
            $(this).removeClass("open");
            $(this).addClass("close")
        })
    }
    if ($(".section305 .spoiler").size() && typeof(window.tobiz.editor) === "undefined") {
        $(".section305 .spoiler").each(function () {
            $(this).removeClass("open");
            $(this).addClass("close")
        })
    }
    if ($(".extra_info_block .extra_image").size() && typeof(window.tobiz.editor) === "undefined") {
        $(".extra_info_block .extra_image").click(function (h) {
            var f = $(this).children("img").attr("src");
            console.log(f);
            var g = f.split("/");
            var d = "/img/400x400/" + g[3];
            $(this).parent().parent().children("img").attr("src", d)
        })
    }
    $("body").on("mouseenter mouseleave", ".section302 li.level0", function (k) {
        if (window.tobiz.editor) {
            return true
        }
        var o = this;
        if (k.type == "mouseenter") {
            $(this).parent().find("li.level1.visible").hide().removeClass("visible");
            var m = false;
            var g = $(o).position();
            var d = g.left + $(o).outerWidth();
            var l = g.top;
            var j = $(o).outerHeight();
            var f = $(this).closest(".section").css("background");
            var n = 0;
            n = 250;
            $(o).nextAll("li").each(function (e, h) {
                if (!m) {
                    if ($(h).hasClass("level1")) {
                        j = $(h).outerHeight();
                        $(h).show().addClass("visible").css({
                            top: l,
                            left: d,
                            width: n,
                            textAlign: "left"
                        }).hover(function () {
                        });
                        l += j - 1
                    } else {
                        m = true
                    }
                }
            })
        }
    });
    $("body").on("mouseenter mouseleave", ".section302 .menu1", function (d) {
        if (window.tobiz.editor) {
            return true
        }
        if (d.type == "mouseleave") {
            $(this).find("li.level1.visible").hide().removeClass("visible")
        }
    });
    $("body").on("mouseenter mouseleave", ".section116 li.level0", function (k) {
        if (window.tobiz.editor) {
            return true
        }
        var o = this;
        if (k.type == "mouseenter") {
            $(this).parent().find("li.level1.visible").hide().removeClass("visible");
            var m = false;
            var g = $(o).position();
            var d = g.left;
            var l = 0;
            var j = $(o).outerHeight();
            var f = $(this).closest(".section").css("background");
            var n = 0;
            n = 250;
            $(o).nextAll("li").each(function (e, h) {
                if (!m) {
                    if ($(h).hasClass("level1")) {
                        l += $(h).outerHeight() - 1;
                        $(h).show().addClass("visible").css({
                            top: l,
                            left: d,
                            background: f,
                            width: n,
                            textAlign: "left"
                        })
                    } else {
                        m = true
                    }
                }
            })
        }
    });
    $("body").on("mouseenter mouseleave", ".section116 .menu1", function (d) {
        if (window.tobiz.editor) {
            return true
        }
        if (d.type == "mouseleave") {
            $(this).find("li.level1.visible").hide().removeClass("visible")
        }
    });
    $("body").on("mouseenter mouseleave", ".section107   li.level0", function (k) {
        if (window.tobiz.editor) {
            return true
        }
        var o = this;
        if (k.type == "mouseenter") {
            $(this).parent().find("li.level1.visible").hide().removeClass("visible");
            var m = false;
            var g = $(o).position();
            var d = g.left;
            var j = $(o).outerHeight();
            var l = g.top - 5;
            var f = $(this).closest(".section").css("background");
            var n = 0;
            n = 200;
            $(o).nextAll("li").each(function (e, h) {
                if (!m) {
                    if ($(h).hasClass("level1")) {
                        l += $(h).outerHeight() - 1;
                        $(h).show().addClass("visible").css({
                            top: l,
                            left: d,
                            background: f,
                            width: n,
                            textAlign: "left",
                            paddingLeft: "16px"
                        })
                    } else {
                        m = true
                    }
                }
            })
        }
    });
    $("body").on("mouseenter mouseleave", ".section107 .menu1", function (d) {
        if (window.tobiz.editor) {
            return true
        }
        if (d.type == "mouseleave") {
            $(this).find("li.level1.visible").hide().removeClass("visible")
        }
    });
    $("body").on("mouseenter mouseleave", ".section105   li.level0", function (k) {
        if (window.tobiz.editor) {
            return true
        }
        var o = this;
        if (k.type == "mouseenter") {
            $(this).parent().find("li.level1.visible").hide().removeClass("visible");
            var m = false;
            var g = $(o).position();
            var d = g.left;
            var j = $(o).outerHeight();
            var l = g.top - 5;
            var f = $(this).closest(".section").css("background");
            var n = 0;
            n = 170;
            $(o).nextAll("li").each(function (e, h) {
                if (!m) {
                    if ($(h).hasClass("level1")) {
                        l += $(h).outerHeight() - 1;
                        $(h).show().addClass("visible").css({
                            top: l,
                            left: d,
                            background: f,
                            width: n,
                            textAlign: "left",
                            paddingLeft: "16px"
                        })
                    } else {
                        m = true
                    }
                }
            })
        }
    });
    $("body").on("mouseenter mouseleave", ".section105 .menu1", function (d) {
        if (window.tobiz.editor) {
            return true
        }
        if (d.type == "mouseleave") {
            $(this).find("li.level1.visible").hide().removeClass("visible")
        }
    });
    $("body").on("mouseenter mouseleave", ".section311   li.level0", function (k) {
        if (window.tobiz.editor) {
            return true
        }
        var o = this;
        if (k.type == "mouseenter") {
            $(this).parent().find("li.level1.visible").hide().removeClass("visible");
            var m = false;
            var g = $(o).position();
            var d = g.left;
            var j = $(o).outerHeight();
            var l = g.top - 5;
            var f = "rgba(0,0,0,0.5)";
            var n = 0;
            n = 170;
            $(o).nextAll("li").each(function (e, h) {
                if (!m) {
                    if ($(h).hasClass("level1")) {
                        l += $(h).outerHeight() - 1;
                        $(h).show().addClass("visible").css({
                            top: l,
                            left: d,
                            background: f,
                            width: n,
                            textAlign: "left",
                            paddingLeft: "10px"
                        })
                    } else {
                        m = true
                    }
                }
            })
        }
    });
    $("body").on("mouseenter mouseleave", ".section311 .menu1", function (d) {
        if (window.tobiz.editor) {
            return true
        }
        if (d.type == "mouseleave") {
            $(this).find("li.level1.visible").hide().removeClass("visible")
        }
    });
    $("body").on("click", ".section126 .img_1, .section126 .img_2 , .section126 .img_3 , .section126 .img_4 ", function () {
        console.log(1);
        var d = $(this).children("img").data("image");
        $(this).parent().parent().find(".image1").children("img").attr("src", d)
    });
    $("body").on("click", ".section310 .image_box", function () {
        var d = $(this).children("img").data("image");
        $(this).parent().parent().parent().parent().find(".viewport").css({backgroundImage: "url(" + d + ")"})
    });

    function b(g, l) {
        var h = g.find(".image_box").size();
        var e = g.find(".image_box").eq(0).css("marginRight");
        e = e.replace(/px/g, "") * 1;
        var k = g.find(".image_box").eq(0).width() + "";
        k = k.replace(/px/g, "") * 1;
        var d = h * k + (h - 1) * e + e;
        g.find(".image_wrapper").css({width: d + "px"});
        var m = g.find(".section_inner").width() + e - d;
        var f = 0;
        var j = g.find(".image_wrapper").css("left");
        j = j.replace(/px/g, "") * 1;
        if (l == "left") {
            j -= (e + k);
            if (d < g.find(".section_inner").width()) {
                return
            }
            if (j < (-1) * (h * k + (h - 1) * e)) {
                return
            }
        }
        if (l == "right") {
            j += (e + k);
            if (j > 0) {
                j = 0
            }
        }
        g.find(".image_wrapper").css({left: j + "px"})
    }

    $("body").on("click", ".section310 .iamges .go_left", function () {
        b($(this).closest(".section"), "right")
    });
    $("body").on("click", ".section310 .iamges .go_right", function () {
        b($(this).closest(".section"), "left")
    });
    $("body").on("click", ".section116 .menu-toogler", function () {
        $(this).parent().children(".menu1").toggle()
    });
    if ($(".section.animate").size() && typeof(window.tobiz.editor) === "undefined" && $(window).width() > 1000 && parseInt(window.tobiz.t) >= 3) {
        $(".section.animate").each(function () {
            if ($(this).hasClass("section118") || $(this).hasClass("section128") || $(this).hasClass("section149") || $(this).hasClass("section124")) {
                var l = ".col_3";
                var h = $(this).find(l);
                var k = ".arr1";
                var e = $(this).find(k);
                h.css({opacity: 0, position: "relative"});
                var j = $(this);
                var f = j.find(".title, .sub_title");
                f.css({opacity: 0, top: "0", position: "relative"});
                $.each(e, function (m, n) {
                    $(n).find(l).eq(0).css({left: "-200px"});
                    $(n).find(l).eq(2).css({left: "200px"})
                });
                $(window).scroll(function () {
                    $.each(h, function (m, n) {
                        if (($(window).scrollTop() + ($(window).height() / 2)) > ($(n).closest(".arr1").offset().top) || ($(window).scrollTop() + $(window).height() > $(n).closest(".arr1").offset().top + $(n).closest(".arr1").height())) {
                            $(n).animate({opacity: 1, left: 0}, 900);
                            f.animate({opacity: 1, top: 0}, 900)
                        }
                    })
                })
            }
            if ($(this).hasClass("section125") || $(this).hasClass("section250")) {
                var l = ".col_2";
                var h = $(this).find(l);
                var k = ".arr1";
                var e = $(this).find(k);
                h.css({opacity: 0, position: "relative"});
                var j = $(this);
                var f = j.find(".title, .sub_title");
                f.css({opacity: 0, top: "0", position: "relative"});
                $.each(e, function (m, n) {
                    $(n).find(l).eq(0).css({left: "-200px"});
                    $(n).find(l).eq(1).css({left: "200px"})
                });
                $(window).scroll(function () {
                    $.each(h, function (m, n) {
                        if (($(window).scrollTop() + ($(window).height() / 2)) > ($(n).closest(".arr1").offset().top) || ($(window).scrollTop() + $(window).height() > $(n).closest(".arr1").offset().top + $(n).closest(".arr1").height())) {
                            $(n).animate({opacity: 1, left: 0}, 900);
                            f.animate({opacity: 1, top: 0}, 900)
                        }
                    })
                })
            }
            if ($(this).hasClass("section307")) {
                var l = ".col";
                var h = $(this).find(l);
                var k = ".section_inner";
                var e = $(this).find(k);
                h.css({opacity: 0, position: "relative"});
                var j = $(this);
                var f = j.find(".title, .sub_title");
                f.css({opacity: 0, top: "0", position: "relative"});
                $.each(e, function (m, n) {
                    $(n).find(l).eq(0).css({left: "-600px"});
                    $(n).find(l).eq(1).css({left: "-400px"});
                    $(n).find(l).eq(2).css({left: "-200px"})
                });
                $(window).scroll(function () {
                    $.each(h, function (m, n) {
                        if (($(window).scrollTop() + ($(window).height() / 2)) > ($(n).closest(".section_inner").offset().top) || ($(window).scrollTop() + $(window).height() > $(n).closest(".section_inner").offset().top + $(n).closest(".section_inner").height())) {
                            $(n).animate({opacity: 1, left: 0}, 900);
                            f.animate({opacity: 1, top: 0}, 900)
                        }
                    })
                })
            }
            if ($(this).hasClass("section314")) {
                var l = ".col_3";
                var h = $(this).find(l);
                var k = ".section_inner";
                var e = $(this).find(k);
                h.css({opacity: 0, position: "relative"});
                var j = $(this);
                var f = j.find(".title, .sub_title");
                f.css({opacity: 0, top: "0", position: "relative"});
                $.each(e, function (m, n) {
                    $(n).find(l).eq(0).css({left: "-600px"});
                    $(n).find(l).eq(1).css({left: "-400px"});
                    $(n).find(l).eq(2).css({left: "-200px"})
                });
                $(window).scroll(function () {
                    $.each(h, function (m, n) {
                        if (($(window).scrollTop() + ($(window).height() / 2)) > ($(n).closest(".section_inner").offset().top) || ($(window).scrollTop() + $(window).height() > $(n).closest(".section_inner").offset().top + $(n).closest(".section_inner").height())) {
                            $(n).animate({opacity: 1, left: 0}, 900);
                            f.animate({opacity: 1, top: 0}, 900)
                        }
                    })
                })
            }
            if ($(this).hasClass("section309")) {
                var l = ".col";
                var h = $(this).find(l);
                var k = ".section_inner";
                var e = $(this).find(k);
                h.css({opacity: 0, position: "relative"});
                var j = $(this);
                var f = j.find(".title, .sub_title");
                f.css({opacity: 0, top: "0", position: "relative"});
                $.each(e, function (m, n) {
                    $(n).find(l).eq(0).css({left: "-1000px"});
                    $(n).find(l).eq(1).css({left: "-800px"});
                    $(n).find(l).eq(2).css({left: "-600px"});
                    $(n).find(l).eq(3).css({left: "-400px"});
                    $(n).find(l).eq(4).css({left: "-200px"})
                });
                $(window).scroll(function () {
                    $.each(h, function (m, n) {
                        if (($(window).scrollTop() + ($(window).height() / 2)) > ($(n).closest(".section_inner").offset().top) || ($(window).scrollTop() + $(window).height() > $(n).closest(".section_inner").offset().top + $(n).closest(".section_inner").height())) {
                            $(n).animate({opacity: 1, left: 0}, 900);
                            f.animate({opacity: 1, top: 0}, 900)
                        }
                    })
                })
            }
            if ($(this).hasClass("section308")) {
                var l = ".col";
                var h = $(this).find(l);
                var k = ".section_inner";
                var e = $(this).find(k);
                h.css({opacity: 0, position: "relative"});
                var j = $(this);
                var f = j.find(".title, .sub_title");
                f.css({opacity: 0, top: "0", position: "relative"});
                $.each(e, function (m, n) {
                    $(n).find(l).eq(0).css({left: "-800px"});
                    $(n).find(l).eq(1).css({left: "-600px"});
                    $(n).find(l).eq(2).css({left: "-400px"});
                    $(n).find(l).eq(3).css({left: "-200px"})
                });
                $(window).scroll(function () {
                    $.each(h, function (m, n) {
                        if (($(window).scrollTop() + ($(window).height() / 2)) > ($(n).closest(".section_inner").offset().top) || ($(window).scrollTop() + $(window).height() > $(n).closest(".section_inner").offset().top + $(n).closest(".section_inner").height())) {
                            $(n).animate({opacity: 1, left: 0}, 900);
                            f.animate({opacity: 1, top: 0}, 900)
                        }
                    })
                })
            }
            if ($(this).hasClass("section226")) {
                var l = ".arr1";
                var h = $(this).find(l);
                var k = ".section_inner";
                var e = $(this).find(k);
                h.css({opacity: 0, position: "relative"});
                var j = $(this);
                var f = j.find(".title, .sub_title");
                f.css({opacity: 0, top: "0", position: "relative"});
                $.each(h, function (m, n) {
                    $(n).css({left: "-" + ((m + 1) * 400) + "px"})
                });
                $(window).scroll(function () {
                    $.each(h, function (m, n) {
                        if (($(window).scrollTop() + ($(window).height() / 2)) > ($(n).closest(".section_inner").offset().top) || ($(window).scrollTop() + $(window).height() > $(n).closest(".section_inner").offset().top + $(n).closest(".section_inner").height())) {
                            $(n).animate({opacity: 1, left: 0}, 900);
                            f.animate({opacity: 1, top: 0}, 900)
                        }
                    })
                })
            }
            if ($(this).hasClass("section132")) {
                var l = ".text";
                var h = $(this).find(l);
                var k = ".section_inner";
                var e = $(this).find(k);
                h.css({opacity: 0, position: "relative"});
                h.eq(0).css({left: "-" + (400) + "px"});
                var j = $(this);
                var f = j.find(".title, .sub_title");
                f.css({opacity: 0, top: "0", position: "relative"});
                $(window).scroll(function () {
                    $.each(h, function (m, n) {
                        if (($(window).scrollTop() + ($(window).height() / 2)) > ($(n).closest(".section_inner").offset().top) || ($(window).scrollTop() + $(window).height() > $(n).closest(".section_inner").offset().top + $(n).closest(".section_inner").height())) {
                            $(n).animate({opacity: 1, left: 0}, 900);
                            f.animate({opacity: 1, top: 0}, 900)
                        }
                    })
                })
            }
            if ($(this).hasClass("section164")) {
                var l = ".pcenter";
                var h = $(this).find(l);
                var k = ".section_inner";
                var e = $(this).find(k);
                h.css({opacity: 0, position: "relative"});
                h.eq(0).css({left: "-" + (400) + "px"});
                var j = $(this);
                var f = j.find(".title, .sub_title");
                f.css({opacity: 0, top: "0", position: "relative"});
                $(window).scroll(function () {
                    $.each(h, function (m, n) {
                        if (($(window).scrollTop() + ($(window).height() / 2)) > ($(n).closest(".section_inner").offset().top) || ($(window).scrollTop() + $(window).height() > $(n).closest(".section_inner").offset().top + $(n).closest(".section_inner").height())) {
                            $(n).animate({opacity: 1, left: 0}, 900);
                            f.animate({opacity: 1, top: 0}, 900)
                        }
                    })
                })
            }
            if ($(this).hasClass("section310") || $(this).hasClass("section148") || $(this).hasClass("section147") || $(this).hasClass("section143") || $(this).hasClass("section306")) {
                var l = ".viewport, .iamges, .image_box1, .image_box2, .image_box3, .image_box4, .image_box5, .image_box, .form_wrapper";
                var h = $(this).find(l);
                var k = ".section_inner";
                var e = $(this).find(k);
                h.css({opacity: 0});
                var j = $(this);
                var f = j.find(".title, .sub_title");
                f.css({opacity: 0});
                $(window).scroll(function () {
                    $.each(h, function (m, n) {
                        if (($(window).scrollTop() + ($(window).height() / 2)) > ($(n).closest(".section_inner").offset().top) || ($(window).scrollTop() + $(window).height() > $(n).closest(".section_inner").offset().top + $(n).closest(".section_inner").height())) {
                            $(n).animate({opacity: 1}, 900);
                            f.animate({opacity: 1}, 900)
                        }
                    })
                })
            }
            if ($(this).hasClass("section126")) {
                var l = ".image1, .video1, .right";
                var g = ".extra_images";
                var h = $(this).find(l);
                var d = $(this).find(g);
                var k = ".arr1";
                var e = $(this).find(k);
                h.css({opacity: 0, position: "relative"});
                d.css({opacity: 0});
                $.each(e, function (m, n) {
                    $(n).find(l).eq(0).css({left: "-200px"});
                    $(n).find(l).eq(1).css({left: "200px"})
                });
                var j = $(this);
                var f = j.find(".title, .sub_title");
                f.css({opacity: 0, top: "0", position: "relative"});
                $(window).scroll(function () {
                    $.each(h, function (m, n) {
                        if (($(window).scrollTop() + ($(window).height() / 2)) > ($(n).closest(".arr1").offset().top) || ($(window).scrollTop() + $(window).height() > $(n).closest(".arr1").offset().top + $(n).closest(".arr1").height())) {
                            $(n).animate({opacity: 1, left: 0}, 900);
                            f.animate({opacity: 1, top: 0}, 900)
                        }
                    });
                    $.each(d, function (m, n) {
                        if (($(window).scrollTop() + ($(window).height() / 2)) > ($(n).closest(".arr1").offset().top) || ($(window).scrollTop() + $(window).height() > $(n).closest(".arr1").offset().top + $(n).closest(".arr1").height())) {
                            $(n).animate({opacity: 1}, 2000)
                        }
                    })
                })
            }
            if ($(this).hasClass("section301")) {
                var l = ".arr1";
                var h = $(this).find(l);
                var k = ".section_inner";
                var e = $(this).find(k);
                h.css({opacity: 0, left: "-400px", position: "relative"});
                var j = $(this);
                var f = j.find(".title, .sub_title");
                f.css({opacity: 0, top: "0", position: "relative"});
                $(window).scroll(function () {
                    $.each(h, function (m, n) {
                        if (($(window).scrollTop() + ($(window).height() / 2)) > ($(n).closest(".arr1").offset().top) || ($(window).scrollTop() + $(window).height() > $(n).closest(".arr1").offset().top + $(n).closest(".arr1").height())) {
                            $(n).animate({opacity: 1, left: 0}, 900);
                            f.animate({opacity: 1, top: 0}, 900)
                        }
                    })
                })
            }
            if ($(this).hasClass("section117")) {
                var l = ".col_2";
                var h = $(this).find(l);
                var k = ".arr1";
                var e = $(this).find(k);
                h.css({opacity: 0, position: "relative"});
                var j = $(this);
                var f = j.find(".title, .sub_title");
                f.css({opacity: 0, top: "0", position: "relative"});
                $.each(e, function (m, n) {
                    if ((m + 1) % 2) {
                        $(n).find(l).eq(0).css({left: "-200px"});
                        $(n).find(l).eq(1).css({left: "200px"})
                    } else {
                        $(n).find(l).eq(0).css({left: "200px"});
                        $(n).find(l).eq(1).css({left: "-200px"})
                    }
                });
                $(window).scroll(function () {
                    $.each(h, function (m, n) {
                        if (($(window).scrollTop() + ($(window).height() / 2)) > ($(n).closest(".arr1").offset().top) || ($(window).scrollTop() + $(window).height() > $(n).closest(".arr1").offset().top + $(n).closest(".arr1").height())) {
                            $(n).animate({opacity: 1, left: 0}, 900);
                            f.animate({opacity: 1, top: 0}, 900)
                        }
                    })
                })
            }
            if ($(this).hasClass("section152") || $(this).hasClass("section201")) {
                var l = ".image_box1, .image_box2, .image_box3, .image_box4, .image_box5, .col_5 ";
                var h = $(this).find(l);
                var k = ".arr1";
                var e = $(this).find(k);
                h.css({opacity: 0, position: "relative"});
                var j = $(this);
                var f = j.find(".title, .sub_title");
                f.css({opacity: 0, top: "0", position: "relative"});
                $.each(e, function (m, n) {
                    $(n).find(l).eq(0).css({left: "-200px"});
                    $(n).find(l).eq(1).css({left: "-100px"});
                    $(n).find(l).eq(3).css({left: "100px"});
                    $(n).find(l).eq(4).css({left: "200px"})
                });
                $(window).scroll(function () {
                    $.each(h, function (m, n) {
                        if (($(window).scrollTop() + ($(window).height() / 2)) > ($(n).closest(".arr1").offset().top) || ($(window).scrollTop() + $(window).height() > $(n).closest(".arr1").offset().top + $(n).closest(".arr1").height())) {
                            $(n).animate({opacity: 1, left: 0, top: 0}, 900);
                            f.animate({opacity: 1, top: 0}, 900)
                        }
                    })
                })
            }
            if ($(this).hasClass("section121") || $(this).hasClass("section151") || $(this).hasClass("section200")) {
                var l = ".col_4";
                var h = $(this).find(l);
                var k = ".arr1";
                var e = $(this).find(k);
                h.css({opacity: 0, position: "relative"});
                var j = $(this);
                var f = j.find(".title, .sub_title");
                f.css({opacity: 0, top: "0", position: "relative"});
                $.each(e, function (m, n) {
                    $(n).find(l).eq(0).css({left: "-200px"});
                    $(n).find(l).eq(1).css({left: "-100px"});
                    $(n).find(l).eq(2).css({left: "100px"});
                    $(n).find(l).eq(3).css({left: "200px"})
                });
                $(window).scroll(function () {
                    $.each(h, function (m, n) {
                        if (($(window).scrollTop() + ($(window).height() / 2)) > ($(n).closest(".arr1").offset().top) || ($(window).scrollTop() + $(window).height() > $(n).closest(".arr1").offset().top + $(n).closest(".arr1").height())) {
                            $(n).animate({opacity: 1, left: 0, top: 0}, 900);
                            f.animate({opacity: 1, top: 0}, 900)
                        }
                    })
                })
            }
        })
    }
    if ($(".section.widget").size() && typeof(window.tobiz.editor) === "undefined" && parseInt(window.tobiz.w) == 1) {
        if ($(".section1000").size()) {
            var a = 1;
            $("html").mouseleave(function (d) {
                if (a) {
                    a = 0;
                    $(".section1000").find(".extra_info_block_wrapper").show()
                }
            })
        }
        if ($(".section1003").size()) {
            $(".section1003").show()
        }
        if ($(".section1001").size()) {
            var c = new Date();
            $(".section1001").each(function () {
                var g = parseInt($(this).data("lag"));
                var f = parseInt($(this).data("start"));
                var d = parseInt($(this).data("stop"));
                if (f < 0 && f > 23) {
                    console.log("Неверные параметры POPUP");
                    return false
                }
                if (d < 0 && d > 23) {
                    console.log("Неверные параметры POPUP");
                    return false
                }
                if (f > d) {
                    console.log("Неверные параметры POPUP");
                    return false
                }
                if (c.getHours() >= f && c.getHours() <= d) {
                    var e = $(this).find(".extra_info_block_wrapper");
                    setTimeout(function () {
                        e.show()
                    }, g * 1000)
                }
            })
        }
    }
    window.tobiz.slider126array = new Object();
    window.tobiz.slider126Init = function (g) {
        var e = 0;
        var f = 1;
        if (!$("#b_" + g).hasClass("slider")) {
            return true
        }
        if ($("#b_" + g).size()) {
            f = $("#b_" + g).data("autoslide") * 1
        }
        if (!(typeof(window.tobiz.slider126array[g]) === "undefined")) {
            if (!(typeof(window.tobiz.slider126array[g].current_id) === "undefined")) {
                e = window.tobiz.slider126array[g].current_id
            }
            f = window.tobiz.slider126array[g].auto_slide;
            delete window.tobiz.slider126array[g]
        }
        if ($("#b_" + g).size()) {
            var d = $("#b_" + g);
            window.tobiz.slider126array[g] = {
                len: d.find(".slide").size(),
                current_id: 0,
                auto_slide: d.data("autoslide") * 1,
                auto_slide_interval: "timeout",
                lag: 4000,
                speed: 500,
                setScroller: function () {
                    d.find(".scroller").html("");
                    var h = d.find(".slide").size();
                    $.each(d.find(".slide"), function (j, k) {
                        d.find(".scroller").append('<div class="point" data-id="' + j + '"></div>')
                    })
                },
                goToSlide: function (l, h, j) {
                    var k = this.speed;
                    if (h == 1) {
                        k = 0
                    }
                    if (j != "right") {
                        j = "left"
                    }
                    if (l < 0) {
                        l = this.len - 1
                    }
                    if (l > this.len - 1) {
                        l = 0
                    }
                    this.current_id = l;
                    if (j == "left") {
                        d.find(".point").hide();
                        d.find(".slider_wrapper").animate({left: "-100%"}, k, function () {
                            d.find(".slider_wrapper").css({left: "100%"});
                            d.find(".slide").hide();
                            d.find(".slide").eq(l).show();
                            d.find(".point").removeClass("current");
                            d.find(".point").eq(l).addClass("current");
                            d.find(".slider_wrapper").animate({left: "0%"}, k, function () {
                                d.find(".point").show()
                            })
                        })
                    } else {
                        d.find(".point").hide();
                        d.find(".slider_wrapper").animate({left: "+100%"}, k, function () {
                            d.find(".slider_wrapper").css({left: "-100%"});
                            d.find(".slide").hide();
                            d.find(".slide").eq(l).show();
                            d.find(".point").removeClass("current");
                            d.find(".point").eq(l).addClass("current");
                            d.find(".slider_wrapper").animate({left: "0%"}, k, function () {
                                d.find(".point").show()
                            })
                        })
                    }
                },
                autoSlide: function () {
                    if (this.auto_slide == 1 && $(window).width() > 900) {
                        var h = this.current_id;
                        h++;
                        if (h > this.len - 1) {
                            h = 0
                        }
                        this.goToSlide(h)
                    }
                },
                eventListener: function () {
                    var h = this;
                    d.find(".point").click(function () {
                        if (!$(this).hasClass("current")) {
                            h.goToSlide($(this).data("id"))
                        }
                        h.auto_slide = 0;
                        clearInterval(h.auto_slide_interval)
                    });
                    d.find(".go_left, .go_right").click(function () {
                        var k = h.current_id;
                        if ($(this).hasClass("go_left")) {
                            k--;
                            if (k < 0) {
                                k = h.len - 1
                            }
                        } else {
                            k++;
                            if (k > h.len - 1) {
                                k = 0
                            }
                        }
                        var j = "left";
                        if ($(this).hasClass("go_right")) {
                            j = "right"
                        }
                        h.goToSlide(k, h.speed, j);
                        h.auto_slide = 0;
                        clearInterval(h.auto_slide_interval)
                    })
                },
                init: function (j, k) {
                    this.auto_slide = k;
                    d.find(".point").eq(0).addClass("current");
                    d.find(".slide").hide();
                    d.find(".slide").eq(0).show();
                    this.setScroller();
                    this.eventListener();
                    this.goToSlide(j, 1);
                    var h = this;
                    if (typeof(window.tobiz.editor) === "undefined") {
                        h.auto_slide_interval = setInterval(function () {
                            h.autoSlide()
                        }, h.lag)
                    }
                }
            };
            window.tobiz.slider126array[g].init(e, f)
        }
    };
    $(".section126").each(function () {
        console.log($(this));
        window.tobiz.slider126Init($(this).data("id"))
    });
    window.tobiz.slider151array = new Object();
    window.tobiz.slider151Init = function (h) {
        var e = 0;
        var g = 1;
        if (!$("#b_" + h).hasClass("slider")) {
            return true
        }
        var f = ".slide";
        if ($(window).width() <= 640) {
            $("#b_" + h).find(f).show();
            f = ".col_4"
        } else {
            $("#b_" + h).find(".col_4").show()
        }
        $("#b_" + h).find(f).eq(0).show();
        if ($("#b_" + h).size()) {
            g = $("#b_" + h).data("autoslide") * 1
        }
        if (!(typeof(window.tobiz.slider151array[h]) === "undefined")) {
            if (!(typeof(window.tobiz.slider151array[h].current_id) === "undefined")) {
                e = window.tobiz.slider151array[h].current_id
            }
            g = window.tobiz.slider151array[h].auto_slide;
            delete window.tobiz.slider151array[h]
        }
        if ($("#b_" + h).size()) {
            var d = $("#b_" + h);
            window.tobiz.slider151array[h] = {
                len: d.find(f).size(),
                current_id: 0,
                auto_slide: d.data("autoslide") * 1,
                auto_slide_interval: "timeout",
                lag: 4000,
                speed: 500,
                setScroller: function () {
                    d.find(".scroller").html("");
                    var j = d.find(f).size();
                    $.each(d.find(f), function (k, l) {
                        d.find(".scroller").append('<div class="point" data-id="' + k + '"></div>')
                    })
                },
                goToSlide: function (m, j, k) {
                    var l = this.speed;
                    if (j == 1) {
                        l = 0
                    }
                    if (k != "right") {
                        k = "left"
                    }
                    if (m < 0) {
                        m = this.len - 1
                    }
                    if (m > this.len - 1) {
                        m = 0
                    }
                    this.current_id = m;
                    if (k == "left") {
                        d.find(".slider_wrapper").animate({left: "-100%"}, l, function () {
                            d.find(".slider_wrapper").css({left: "100%"});
                            d.find(f).hide();
                            d.find(f).eq(m).show();
                            d.find(".point").removeClass("current");
                            d.find(".point").eq(m).addClass("current");
                            d.find(".slider_wrapper").animate({left: "0%"}, l)
                        })
                    } else {
                        d.find(".slider_wrapper").animate({left: "+100%"}, l, function () {
                            d.find(".slider_wrapper").css({left: "-100%"});
                            d.find(f).hide();
                            d.find(f).eq(m).show();
                            d.find(".point").removeClass("current");
                            d.find(".point").eq(m).addClass("current");
                            d.find(".slider_wrapper").animate({left: "0%"}, l)
                        })
                    }
                },
                autoSlide: function () {
                    if (this.auto_slide == 1 && $(window).width() > 900) {
                        var j = this.current_id;
                        j++;
                        if (j > this.len - 1) {
                            j = 0
                        }
                        this.goToSlide(j)
                    }
                },
                eventListener: function () {
                    var j = this;
                    d.find(".point").click(function () {
                        if (!$(this).hasClass("current")) {
                            j.goToSlide($(this).data("id"))
                        }
                        j.auto_slide = 0;
                        clearInterval(j.auto_slide_interval)
                    });
                    d.find(".go_left, .go_right").click(function () {
                        var l = j.current_id;
                        if ($(this).hasClass("go_left")) {
                            l--;
                            if (l < 0) {
                                l = j.len - 1
                            }
                        } else {
                            l++;
                            if (l > j.len - 1) {
                                l = 0
                            }
                        }
                        var k = "left";
                        if ($(this).hasClass("go_right")) {
                            k = "right"
                        }
                        j.goToSlide(l, j.speed, k);
                        j.auto_slide = 0;
                        clearInterval(j.auto_slide_interval)
                    })
                },
                init: function (k, l) {
                    this.auto_slide = l;
                    d.find(".point").eq(0).addClass("current");
                    d.find(f).hide();
                    d.find(f).eq(0).show();
                    this.setScroller();
                    this.eventListener();
                    this.goToSlide(k, 1);
                    var j = this;
                    if (typeof(window.tobiz.editor) === "undefined") {
                        j.auto_slide_interval = setInterval(function () {
                            j.autoSlide()
                        }, j.lag)
                    }
                }
            };
            window.tobiz.slider151array[h].init(e, g)
        }
    };
    $(".section151").each(function () {
        console.log($(this));
        window.tobiz.slider151Init($(this).data("id"))
    });
    window.tobiz.slider320array = new Object();
    window.tobiz.slider320Init = function (h) {
        var e = 0;
        var g = 1;
        if (!$("#b_" + h).hasClass("slider")) {
            return true
        }
        var f = ".slide";
        if ($(window).width() <= 640) {
            $("#b_" + h).find(f).show();
            f = ".col_4"
        } else {
            $("#b_" + h).find(".col_4").show()
        }
        $("#b_" + h).find(f).eq(0).show();
        if ($("#b_" + h).size()) {
            g = $("#b_" + h).data("autoslide") * 1
        }
        if (!(typeof(window.tobiz.slider320array[h]) === "undefined")) {
            if (!(typeof(window.tobiz.slider320array[h].current_id) === "undefined")) {
                e = window.tobiz.slider320array[h].current_id
            }
            g = window.tobiz.slider320array[h].auto_slide;
            delete window.tobiz.slider320array[h]
        }
        if ($("#b_" + h).size()) {
            var d = $("#b_" + h);
            window.tobiz.slider320array[h] = {
                len: d.find(f).size(),
                current_id: 0,
                auto_slide: d.data("autoslide") * 1,
                auto_slide_interval: "timeout",
                lag: 4000,
                speed: 500,
                setScroller: function () {
                    d.find(".scroller").html("");
                    var j = d.find(f).size();
                    $.each(d.find(f), function (k, l) {
                        d.find(".scroller").append('<div class="point" data-id="' + k + '"></div>')
                    })
                },
                goToSlide: function (m, j, k) {
                    var l = this.speed;
                    if (j == 1) {
                        l = 0
                    }
                    if (k != "right") {
                        k = "left"
                    }
                    if (m < 0) {
                        m = this.len - 1
                    }
                    if (m > this.len - 1) {
                        m = 0
                    }
                    this.current_id = m;
                    if (k == "left") {
                        d.find(".slider_wrapper").animate({left: "-100%"}, l, function () {
                            d.find(".slider_wrapper").css({left: "100%"});
                            d.find(f).hide();
                            d.find(f).eq(m).show();
                            d.find(".point").removeClass("current");
                            d.find(".point").eq(m).addClass("current");
                            d.find(".slider_wrapper").animate({left: "0%"}, l)
                        })
                    } else {
                        d.find(".slider_wrapper").animate({left: "+100%"}, l, function () {
                            d.find(".slider_wrapper").css({left: "-100%"});
                            d.find(f).hide();
                            d.find(f).eq(m).show();
                            d.find(".point").removeClass("current");
                            d.find(".point").eq(m).addClass("current");
                            d.find(".slider_wrapper").animate({left: "0%"}, l)
                        })
                    }
                },
                autoSlide: function () {
                    if (this.auto_slide == 1 && $(window).width() > 900) {
                        var j = this.current_id;
                        j++;
                        if (j > this.len - 1) {
                            j = 0
                        }
                        this.goToSlide(j)
                    }
                },
                eventListener: function () {
                    var j = this;
                    d.find(".point").click(function () {
                        if (!$(this).hasClass("current")) {
                            j.goToSlide($(this).data("id"))
                        }
                        j.auto_slide = 0;
                        clearInterval(j.auto_slide_interval)
                    });
                    d.find(".go_left, .go_right").click(function () {
                        var l = j.current_id;
                        if ($(this).hasClass("go_left")) {
                            l--;
                            if (l < 0) {
                                l = j.len - 1
                            }
                        } else {
                            l++;
                            if (l > j.len - 1) {
                                l = 0
                            }
                        }
                        var k = "left";
                        if ($(this).hasClass("go_right")) {
                            k = "right"
                        }
                        j.goToSlide(l, j.speed, k);
                        j.auto_slide = 0;
                        clearInterval(j.auto_slide_interval)
                    })
                },
                init: function (k, l) {
                    this.auto_slide = l;
                    d.find(".point").eq(0).addClass("current");
                    d.find(f).hide();
                    d.find(f).eq(0).show();
                    this.setScroller();
                    this.eventListener();
                    this.goToSlide(k, 1);
                    var j = this;
                    if (typeof(window.tobiz.editor) === "undefined") {
                        j.auto_slide_interval = setInterval(function () {
                            j.autoSlide()
                        }, j.lag)
                    }
                }
            };
            window.tobiz.slider320array[h].init(e, g)
        }
    };
    $(".section320").each(function () {
        console.log($(this));
        window.tobiz.slider320Init($(this).data("id"))
    });
    window.tobiz.slider149array = new Object();
    window.tobiz.slider149Init = function (h) {
        var e = 0;
        var g = 1;
        if (!$("#b_" + h).hasClass("slider")) {
            return true
        }
        var f = ".slide";
        if ($(window).width() <= 640) {
            $("#b_" + h).find(f).show();
            f = ".col_3"
        } else {
            $("#b_" + h).find(".col_3").show()
        }
        $("#b_" + h).find(f).eq(0).show();
        if ($("#b_" + h).size()) {
            g = $("#b_" + h).data("autoslide") * 1
        }
        if (!(typeof(window.tobiz.slider149array[h]) === "undefined")) {
            if (!(typeof(window.tobiz.slider149array[h].current_id) === "undefined")) {
                e = window.tobiz.slider149array[h].current_id
            }
            g = window.tobiz.slider149array[h].auto_slide;
            delete window.tobiz.slider149array[h]
        }
        if ($("#b_" + h).size()) {
            var d = $("#b_" + h);
            window.tobiz.slider149array[h] = {
                len: d.find(f).size(),
                current_id: 0,
                auto_slide: d.data("autoslide") * 1,
                auto_slide_interval: "timeout",
                lag: 4000,
                speed: 500,
                setScroller: function () {
                    d.find(".scroller").html("");
                    var j = d.find(f).size();
                    $.each(d.find(f), function (k, l) {
                        d.find(".scroller").append('<div class="point" data-id="' + k + '"></div>')
                    })
                },
                goToSlide: function (m, j, k) {
                    var l = this.speed;
                    if (j == 1) {
                        l = 0
                    }
                    if (k != "right") {
                        k = "left"
                    }
                    if (m < 0) {
                        m = this.len - 1
                    }
                    if (m > this.len - 1) {
                        m = 0
                    }
                    this.current_id = m;
                    if (k == "left") {
                        d.find(".point").hide();
                        d.find(".slider_wrapper").animate({left: "-100%"}, l, function () {
                            d.find(".slider_wrapper").css({left: "100%"});
                            d.find(f).hide();
                            d.find(f).eq(m).show();
                            d.find(".point").removeClass("current");
                            d.find(".point").eq(m).addClass("current");
                            d.find(".slider_wrapper").animate({left: "0%"}, l, function () {
                                d.find(".point").show()
                            })
                        })
                    } else {
                        d.find(".point").hide();
                        d.find(".slider_wrapper").animate({left: "+100%"}, l, function () {
                            d.find(".slider_wrapper").css({left: "-100%"});
                            d.find(f).hide();
                            d.find(f).eq(m).show();
                            d.find(".point").removeClass("current");
                            d.find(".point").eq(m).addClass("current");
                            d.find(".slider_wrapper").animate({left: "0%"}, l, function () {
                                d.find(".point").show()
                            })
                        })
                    }
                },
                autoSlide: function () {
                    if (this.auto_slide == 1 && $(window).width() > 900) {
                        var j = this.current_id;
                        j++;
                        if (j > this.len - 1) {
                            j = 0
                        }
                        this.goToSlide(j)
                    }
                },
                eventListener: function () {
                    var j = this;
                    d.find(".point").click(function () {
                        if (!$(this).hasClass("current")) {
                            j.goToSlide($(this).data("id"))
                        }
                        j.auto_slide = 0;
                        clearInterval(j.auto_slide_interval)
                    });
                    d.find(".go_left, .go_right").click(function () {
                        var l = j.current_id;
                        if ($(this).hasClass("go_left")) {
                            l--;
                            if (l < 0) {
                                l = j.len - 1
                            }
                        } else {
                            l++;
                            if (l > j.len - 1) {
                                l = 0
                            }
                        }
                        var k = "left";
                        if ($(this).hasClass("go_right")) {
                            k = "right"
                        }
                        j.goToSlide(l, j.speed, k);
                        j.auto_slide = 0;
                        clearInterval(j.auto_slide_interval)
                    })
                },
                init: function (k, l) {
                    this.auto_slide = l;
                    d.find(".point").eq(0).addClass("current");
                    d.find(f).hide();
                    d.find(f).eq(0).show();
                    this.setScroller();
                    this.eventListener();
                    this.goToSlide(k, 1);
                    var j = this;
                    if (typeof(window.tobiz.editor) === "undefined") {
                        j.auto_slide_interval = setInterval(function () {
                            j.autoSlide()
                        }, j.lag)
                    }
                }
            };
            window.tobiz.slider149array[h].init(e, g)
        }
    };
    $(".section149").each(function () {
        console.log($(this));
        window.tobiz.slider149Init($(this).data("id"))
    });
    window.tobiz.slider118array = new Object();
    window.tobiz.slider118Init = function (h) {
        var e = 0;
        var g = 1;
        if (!$("#b_" + h).hasClass("slider")) {
            return true
        }
        var f = ".slide";
        if ($(window).width() <= 800) {
            $("#b_" + h).find(f).show();
            f = ".col_3"
        } else {
            $("#b_" + h).find(".col_3").show()
        }
        $("#b_" + h).find(f).eq(0).show();
        if ($("#b_" + h).size()) {
            g = $("#b_" + h).data("autoslide") * 1
        }
        if (!(typeof(window.tobiz.slider118array[h]) === "undefined")) {
            if (!(typeof(window.tobiz.slider118array[h].current_id) === "undefined")) {
                e = window.tobiz.slider118array[h].current_id
            }
            g = window.tobiz.slider118array[h].auto_slide;
            delete window.tobiz.slider118array[h]
        }
        if ($("#b_" + h).size()) {
            var d = $("#b_" + h);
            window.tobiz.slider118array[h] = {
                len: d.find(f).size(),
                current_id: 0,
                auto_slide: d.data("autoslide") * 1,
                auto_slide_interval: "timeout",
                lag: 4000,
                speed: 500,
                setScroller: function () {
                    d.find(".scroller").html("");
                    var j = d.find(f).size();
                    $.each(d.find(f), function (k, l) {
                        d.find(".scroller").append('<div class="point" data-id="' + k + '"></div>')
                    })
                },
                goToSlide: function (m, j, k) {
                    var l = this.speed;
                    if (j == 1) {
                        l = 0
                    }
                    if (k != "right") {
                        k = "left"
                    }
                    if (m < 0) {
                        m = this.len - 1
                    }
                    if (m > this.len - 1) {
                        m = 0
                    }
                    this.current_id = m;
                    if (k == "left") {
                        d.find(".point").hide();
                        d.find(".slider_wrapper").animate({left: "-100%"}, l, function () {
                            d.find(".slider_wrapper").css({left: "100%"});
                            d.find(f).hide();
                            d.find(f).eq(m).show();
                            d.find(".point").removeClass("current");
                            d.find(".point").eq(m).addClass("current");
                            d.find(".slider_wrapper").animate({left: "0%"}, l, function () {
                                d.find(".point").show()
                            })
                        })
                    } else {
                        d.find(".point").hide();
                        d.find(".slider_wrapper").animate({left: "+100%"}, l, function () {
                            d.find(".slider_wrapper").css({left: "-100%"});
                            d.find(f).hide();
                            d.find(f).eq(m).show();
                            d.find(".point").removeClass("current");
                            d.find(".point").eq(m).addClass("current");
                            d.find(".slider_wrapper").animate({left: "0%"}, l, function () {
                                d.find(".point").show()
                            })
                        })
                    }
                },
                autoSlide: function () {
                    if (this.auto_slide == 1) {
                        var j = this.current_id;
                        j++;
                        if (j > this.len - 1) {
                            j = 0
                        }
                        this.goToSlide(j)
                    }
                },
                eventListener: function () {
                    var j = this;
                    d.find(".point").click(function () {
                        if (!$(this).hasClass("current")) {
                            j.goToSlide($(this).data("id"))
                        }
                        j.auto_slide = 0;
                        clearInterval(j.auto_slide_interval)
                    });
                    d.find(".go_left, .go_right").click(function () {
                        var l = j.current_id;
                        if ($(this).hasClass("go_left")) {
                            l--;
                            if (l < 0) {
                                l = j.len - 1
                            }
                        } else {
                            l++;
                            if (l > j.len - 1) {
                                l = 0
                            }
                        }
                        var k = "left";
                        if ($(this).hasClass("go_right")) {
                            k = "right"
                        }
                        j.goToSlide(l, j.speed, k);
                        j.auto_slide = 0;
                        clearInterval(j.auto_slide_interval)
                    })
                },
                init: function (k, l) {
                    this.auto_slide = l;
                    d.find(".point").eq(0).addClass("current");
                    d.find(f).hide();
                    d.find(f).eq(0).show();
                    this.setScroller();
                    this.eventListener();
                    this.goToSlide(k, 1);
                    var j = this;
                    if (typeof(window.tobiz.editor) === "undefined") {
                        j.auto_slide_interval = setInterval(function () {
                            j.autoSlide()
                        }, j.lag)
                    }
                }
            };
            window.tobiz.slider118array[h].init(e, g)
        }
    };
    $(".section118").each(function () {
        console.log($(this));
        window.tobiz.slider118Init($(this).data("id"))
    });
    window.tobiz.slider312array = new Object();
    window.tobiz.slider312Init = function (g) {
        var e = 0;
        var f = 1;
        if ($("#b_" + g).size()) {
            f = $("#b_" + g).data("autoslide") * 1
        }
        if (!(typeof(window.tobiz.slider312array[g]) === "undefined")) {
            if (!(typeof(window.tobiz.slider312array[g].current_id) === "undefined")) {
                e = window.tobiz.slider312array[g].current_id
            }
            f = window.tobiz.slider312array[g].auto_slide;
            delete window.tobiz.slider312array[g]
        }
        if ($("#b_" + g).size()) {
            var d = $("#b_" + g);
            window.tobiz.slider312array[g] = {
                width: d.find(".section_inner").width(),
                height: d.find(".slider").height(),
                len: d.find(".slide").size(),
                current_id: 0,
                auto_slide: d.data("autoslide") * 1,
                auto_slide_interval: "timeout",
                lag: 6000,
                setScroller: function () {
                    d.find(".scroller").html("");
                    var h = d.find(".slide").size();
                    $.each(d.find(".slide"), function (j, k) {
                        d.find(".scroller").append('<div class="point" data-id="' + j + '"></div>')
                    })
                },
                goToSlide: function (l, h) {
                    var j = 500;
                    if (h == 1) {
                        j = 0
                    }
                    if (l < 0) {
                        l = this.len - 1
                    }
                    if (l > this.len - 1) {
                        l = 0
                    }
                    d.find(".point").removeClass("current");
                    var k = (-1 * (l * this.width)) + "px";
                    d.find(".slider_wrapper").animate({left: k}, j);
                    this.current_id = l;
                    d.find(".point").eq(l).addClass("current")
                },
                autoSlide: function () {
                    if (this.auto_slide == 1) {
                        var h = this.current_id;
                        h++;
                        if (h > this.len - 1) {
                            h = 0
                        }
                        this.goToSlide(h)
                    }
                },
                eventListener: function () {
                    var h = this;
                    d.find(".point").click(function () {
                        if (!$(this).hasClass("current")) {
                            h.goToSlide($(this).data("id"))
                        }
                        h.auto_slide = 0;
                        clearInterval(h.auto_slide_interval)
                    });
                    d.find(".go_left, .go_right").click(function () {
                        var j = h.current_id;
                        if ($(this).hasClass("go_left")) {
                            j--;
                            if (j < 0) {
                                j = h.len - 1
                            }
                        } else {
                            j++;
                            if (j > h.len - 1) {
                                j = 0
                            }
                        }
                        h.goToSlide(j);
                        h.auto_slide = 0;
                        clearInterval(h.auto_slide_interval)
                    })
                },
                init: function (j, k) {
                    this.auto_slide = k;
                    d.find(".point").eq(0).addClass("current");
                    d.find(".slider_wrapper").css({width: this.width * this.len, height: this.height});
                    d.find(".slide").css({width: this.width});
                    d.find(".arr1").css({width: this.width});
                    this.setScroller();
                    this.eventListener();
                    this.goToSlide(j, 1);
                    var h = this;
                    if (typeof(window.tobiz.editor) === "undefined") {
                        h.auto_slide_interval = setInterval(function () {
                            h.autoSlide()
                        }, h.lag)
                    }
                }
            };
            window.tobiz.slider312array[g].init(e, f)
        }
    };
    $(".section312").each(function () {
        console.log($(this));
        window.tobiz.slider312Init($(this).data("id"))
    });
    $(window).resize(function () {
    });
    $(window).resize(function () {
        if (this.resizeTO) {
            clearTimeout(this.resizeTO)
        }
        this.resizeTO = setTimeout(function () {
            $(this).trigger("resizeComplete")
        }, 500)
    });
    $(window).bind("resizeComplete", function () {
        console.log("!");
        $(".section320").each(function () {
            window.tobiz.slider320Init($(this).data("id"))
        });
        $(".section312").each(function () {
            window.tobiz.slider312Init($(this).data("id"))
        });
        $(".section126").each(function () {
            window.tobiz.slider126Init($(this).data("id"))
        });
        $(".section151").each(function () {
            window.tobiz.slider151Init($(this).data("id"))
        });
        $(".section149").each(function () {
            window.tobiz.slider149Init($(this).data("id"))
        });
        $(".section118").each(function () {
            window.tobiz.slider118Init($(this).data("id"))
        });
        if ($(window).width() > 640) {
            $(".section116").find(".menu1").show()
        } else {
            $(".section116").find(".menu1").hide()
        }
    });
    setInterval(function () {
        $({temporary_x: -151, temporary_y: 0}).animate({temporary_x: 500, temporary_y: 0}, {
            duration: 1000,
            step: function () {
                var d = Math.round(this.temporary_x) + "px " + Math.round(this.temporary_y) + "px";
                $(".btn.animation, .btn1.animation, .btn2.animation, .btn3.animation, .btn4.animation, .btn5.animation").css("background-position", d)
            }
        })
    }, 2000)
});